---
 - name: Survey manipulation
   hosts: all
   gather_facts: no
   vars:
    - _disks: "{{ disks.split('\n') }}"
  tasks:
   tasks:
     - name: Show objects
       debug:
         var: _disks
         
        
